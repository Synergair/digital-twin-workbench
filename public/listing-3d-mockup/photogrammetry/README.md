Place generated ODM assets in this directory.

Expected runtime file:
- `manifest.json`

The app will auto-load `manifest.json` if present.

You can generate files here with:

```bash
node scripts/prepare-odm-model.mjs --project odm-data/listing-001
```
